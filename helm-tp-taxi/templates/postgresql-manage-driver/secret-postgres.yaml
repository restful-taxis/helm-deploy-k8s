apiVersion: v1
kind: Secret
metadata:
  name: {{.Values.postgresql_manage_driver.secretName}}
  namespace: {{.Release.Namespace}}

type: Opaque
stringData:
  {{- range $key, $value := .Values.postgresql_manage_driver.credentials }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}