apiVersion: v1
kind: Service
metadata:
    name: {{.Values.api_manage_course.service.name}}
    namespace: {{.Release.Namespace}}
spec:
    type: {{.Values.api_manage_course.service.type}}
    selector:
        app: {{.Values.api_manage_course.deployment.name}}
    ports:
        - port: {{.Values.api_manage_course.service.port}}
          protocol: TCP
          targetPort: {{.Values.api_manage_course.service.targetPort}}