apiVersion: v1
stringData:
    DATABASE_URL: "postgresql://{{.Values.postgresql_manage_course.credentials.user}}:{{.Values.postgresql_manage_course.credentials.password}}@{{.Values.postgresql_manage_course.service.name}}:{{.Values.postgresql_manage_course.service.port}}/{{.Values.postgresql_manage_course.credentials.database}}"
    {{- range $key, $value := .Values.api_manage_course.secret.env }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
kind: Secret
metadata:
    name: {{.Values.api_manage_course.secret.name}}
    namespace: {{.Release.Namespace}}
type: Opaque
