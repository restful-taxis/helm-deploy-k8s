apiVersion: v1
kind: Secret
metadata:
  name: {{.Values.postgresql_manage_course.secretName}}
  namespace: {{.Release.Namespace}}

type: Opaque
stringData:
  {{- range $key, $value := .Values.postgresql_manage_course.credentials }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}