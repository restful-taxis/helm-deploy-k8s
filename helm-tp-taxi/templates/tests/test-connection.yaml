apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-tp-taxi.fullname" . }}-test-connection"
  labels:
    {{- include "helm-tp-taxi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-tp-taxi.fullname" . }}:{{ .Values.tests.service.port }}']
  restartPolicy: Never
